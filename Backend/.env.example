# Example environment variables for the ExamManagementSystem backend
# Copy this file to .env and fill in the real values before starting the server.

# port the server should listen on
PORT=4000

# MongoDB connection string (replace with your own)
DB_URL=mongodb://localhost:27017/exam_management

# secret used for signing JWTs
JWT_SECRET=replace_with_a_strong_secret
